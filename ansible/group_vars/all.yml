---
# Global variables for all hosts

# Common packages to install on all systems
common_packages:
  - vim
  - git
  - htop
  - curl
  - wget

# Timezone
timezone: America/Chicago

# SSH settings
ssh_port: 22
